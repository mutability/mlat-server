Put the html / css / js files somewhere accessible.
Put the sync.json file generated by the server (in its --work-dir) in the same place
  (the simplest way is to create a symlink pointing to the copy in --work-dir)
